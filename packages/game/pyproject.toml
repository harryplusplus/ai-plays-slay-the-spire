[project]
name = "game"
version = "0.0.0"
description = "CLI for AI plays Slay the Spire"
dependencies = [
    "httpx>=0.28.1",
    "typer>=0.24.2",
    "common",
    "hindsight-client>=0.6.0",
]

[project.scripts]
game = "game.cli:main"

[tool.uv]
package = true

[tool.uv.sources]
common = { workspace = true }
