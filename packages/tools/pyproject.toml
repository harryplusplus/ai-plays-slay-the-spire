[project]
name = "tools"
version = "0.0.0"
description = "Tools for AI plays Slay the Spire"
dependencies = ["core>=0.0.0", "typer>=0.24.1"]

[project.scripts]
bootstrap = "tools.bootstrap:main"
build-mod = "tools.build_mod:main"

[tool.uv.sources]
core = { workspace = true }

[tool.uv]
package = true
