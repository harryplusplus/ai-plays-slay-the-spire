[project]
name = "core"
version = "0.0.0"
description = "Core for AI plays Slay the Spire"
dependencies = [
    "aiosqlite>=0.22.1",
]

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
