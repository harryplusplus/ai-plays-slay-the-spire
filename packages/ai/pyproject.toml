[project]
name = "ai"
version = "0.0.0"
description = "AI plays Slay the Spire"
dependencies = [
    "common",
    "openai>=2.32.0",
    "pillow>=12.2.0",
]

[project.scripts]
ai = "ai.main:main"

[tool.uv]
package = true

[tool.uv.sources]
common = { workspace = true }
