[project]
name = "ai"
version = "0.0.0"
description = "AI for playing Slay the Spire"
dependencies = ["core>=0.0.0", "websockets>=16.0"]

[tool.uv.sources]
core = { workspace = true }

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
