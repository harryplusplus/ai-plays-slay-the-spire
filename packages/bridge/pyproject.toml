[project]
name = "bridge"
version = "0.0.0"
description = "Bridge between AI and Slay the Spire"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.46.0",
]

[project.scripts]
bridge = "bridge.main:main"

[tool.uv]
package = true
