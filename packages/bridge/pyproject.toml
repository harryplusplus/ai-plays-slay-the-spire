[project]
name = "bridge"
version = "0.0.0"
description = "Bridge for AI plays Slay the Spire"
dependencies = [
    "core>=0.0.0",
    "fastapi>=0.135.1",
    "uvicorn>=0.42.0",
    "aiosqlite>=0.22.1",
    "sqlalchemy[asyncio]>=2.0.48",
]

[tool.uv.sources]
core = { workspace = true }

[tool.uv]
package = true
